# Git Push 全流程命令行代码

## 🚀 完整的 Git 推送工作流程

### 1. 检查当前状态
```bash
# 查看当前分支和文件状态
git status

# 查看具体的文件变更
git diff

# 查看已暂存的变更
git diff --staged
```

### 2. 添加文件到暂存区
```bash
# 添加所有变更的文件
git add .

# 或者添加特定文件
git add README.md
git add complete_usage_guide.py
git add plotly_visualization_generator.py

# 添加特定类型的文件
git add *.py
git add *.md

# 交互式添加（可以选择性添加文件的部分内容）
git add -i
```

### 3. 提交变更
```bash
# 标准提交（会打开编辑器）
git commit

# 直接提交并添加消息
git commit -m "feat: 添加新功能描述"

# 提交所有已跟踪文件的变更（跳过 git add）
git commit -am "fix: 修复某个问题"

# 修改最后一次提交（如果还没有推送）
git commit --amend -m "更新的提交消息"
```

### 4. 推送到远程仓库
```bash
# 推送到默认远程分支
git push

# 推送到指定远程和分支
git push origin main

# 强制推送（谨慎使用）
git push --force

# 推送并设置上游分支
git push -u origin main
```

## 📋 常用的完整工作流程

### 日常开发推送
```bash
# 1. 检查状态
git status

# 2. 添加所有变更
git add .

# 3. 提交变更
git commit -m "feat: 描述你的变更内容"

# 4. 推送到远程
git push origin main
```

### 推送前的安全检查
```bash
# 1. 查看将要提交的内容
git status
git diff --staged

# 2. 确认远程仓库状态
git remote -v
git branch -a

# 3. 拉取最新变更（避免冲突）
git pull origin main

# 4. 添加并提交
git add .
git commit -m "你的提交消息"

# 5. 推送
git push origin main
```

## 🔧 高级操作

### 处理冲突
```bash
# 如果推送时遇到冲突
git pull origin main
# 解决冲突后
git add .
git commit -m "resolve: 解决合并冲突"
git push origin main
```

### 撤销操作
```bash
# 撤销最后一次提交（保留文件变更）
git reset --soft HEAD~1

# 撤销最后一次提交（丢弃文件变更）
git reset --hard HEAD~1

# 撤销暂存的文件
git reset HEAD <文件名>

# 撤销工作区的变更
git checkout -- <文件名>
```

### 查看历史
```bash
# 查看提交历史
git log --oneline

# 查看详细历史
git log --graph --pretty=format:'%h -%d %s (%cr) <%an>'

# 查看特定文件的历史
git log --follow <文件名>
```

## 🎯 针对你的项目的推送模板

### 功能开发完成后
```bash
# 检查状态
git status

# 添加所有相关文件
git add README.md complete_usage_guide.py plotly_visualization_generator.py *.md

# 提交变更（使用规范的提交消息）
git commit -m "feat: 添加Plotly交互式可视化功能

- 实现PlotlyNetworkVisualizer类
- 集成到主管线菜单系统
- 支持多种布局算法
- 添加网络统计仪表板
- 更新README文档格式"

# 推送到GitHub
git push origin main
```

### 修复问题后
```bash
git add .
git commit -m "fix: 修复可视化生成中的布局问题"
git push origin main
```

### 更新文档后
```bash
git add README.md *.md
git commit -m "docs: 更新项目文档和使用说明"
git push origin main
```

### 重构代码后
```bash
git add .
git commit -m "refactor: 重构网络可视化模块，提升代码可维护性"
git push origin main
```

## 📝 提交消息规范

使用约定式提交（Conventional Commits）格式：

```
<类型>[可选的作用域]: <描述>

[可选的正文]

[可选的脚注]
```

### 常用类型：
- `feat`: 新功能
- `fix`: 修复问题
- `docs`: 文档变更
- `style`: 代码格式变更
- `refactor`: 重构代码
- `test`: 添加或修改测试
- `chore`: 构建过程或辅助工具的变动

### 示例：
```bash
git commit -m "feat: 添加Plotly交互式网络可视化"
git commit -m "fix: 修复子图激活中的内存泄漏问题"
git commit -m "docs: 更新README，添加安装和使用说明"
git commit -m "refactor: 重构可视化生成器，提升性能"
```

## 🚨 注意事项

1. **推送前检查**：始终在推送前检查 `git status` 和 `git diff`
2. **避免推送敏感信息**：检查是否包含密码、API密钥等
3. **保持提交原子性**：每次提交只包含一个逻辑变更
4. **写清楚的提交消息**：让其他人（包括未来的你）能理解变更内容
5. **定期推送**：不要积累太多变更才推送

## 🔄 快速推送脚本

你可以创建一个脚本文件 `quick_push.sh`：

```bash
#!/bin/bash
# 快速推送脚本

echo "🔍 检查当前状态..."
git status

echo "📝 添加所有变更..."
git add .

echo "💬 请输入提交消息："
read commit_message

echo "📤 提交并推送..."
git commit -m "$commit_message"
git push origin main

echo "✅ 推送完成！"
```

使用方法：
```bash
chmod +x quick_push.sh
./quick_push.sh
```

---

**保存这个文件以备将来参考！** 🚀